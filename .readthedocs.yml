version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "3.13"
  apt_packages:
    - pandoc  # Required for nbsphinx to convert notebooks

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev
